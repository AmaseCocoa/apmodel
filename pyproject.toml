[project]
name = "apmodel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AmaseCocoa", email = "cocoa@amase.cc" }
]
requires-python = ">=3.10"
dependencies = [
    "snaplet>=0.1.1",
]

[tool.uv.sources]
snaplet = { git = "https://github.com/AmaseCocoa/snaplet", branch = "master" }

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
]
