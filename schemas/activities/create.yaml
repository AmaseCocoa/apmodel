# yaml-language-server: $schema=../schema.json
imports:
  - path: "..core"
    name: "Activity"

classes:
  Create:
    extends: Activity
    description: "Indicates that the actor has created the object."
    mixins: