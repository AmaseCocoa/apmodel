# yaml-language-server: $schema=./schema.json
imports:

classes:
  PropertyValue:
    extends: null
    isCoreType: false
    as2Uri: "http://schema.org#PropertyValue"
    description: ""
    contextRules:
      when_field_exists:
        - value
      context:
        - http://schema.org#
        - schema: http://schema.org#
          value: schema:value
    properties:
      name: 
         type: str | None
      value: 
          type: str | None
