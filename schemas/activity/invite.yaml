# yaml-language-server: $schema=../schema.json
imports:
  - path: "apmodel.activity.offer"
    name: "Offer"

classes:
  Invite:
    extends: Offer
    description: A specialization of Offer in which the actor is extending an invitation for the object to the target.
    mixins: