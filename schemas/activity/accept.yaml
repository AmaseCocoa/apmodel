# yaml-language-server: $schema=../schema.json
imports:
  - path: "apmodel.core"
    name: "Activity"

classes:
  Accept:
    extends: Activity
    mixins:
  TentativeAccept:
    extends: Accept
    mixins: