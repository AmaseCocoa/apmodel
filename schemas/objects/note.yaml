# yaml-language-server: $schema=../schema.json
imports:
  - path: "apmodel.core"
    name: "Collection, Object"

classes:
  Note:
    extends: Object
    properties:
      likes:
         type: Collection | None
      shares:
         type: Collection | None