# yaml-language-server: $schema=../schema.json
imports:
  - path: "apmodel.core"
    name: "Link"

classes:
  Mention:
    extends: Link
    description: A specialized Link that represents an @mention.
    mixins: