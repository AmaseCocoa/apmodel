# yaml-language-server: $schema=../schema.json
imports:
  - path: "apmodel.core"
    name: "Object"

classes:
  Profile:
    extends: Object
    mixins:
    properties:
      describes:
        description: ""